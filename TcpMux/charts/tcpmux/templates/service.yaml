apiVersion: v1
kind: Service
metadata:
  name: {{ template "tcpmux.fullname" . }}
  labels:
    app: {{ template "tcpmux.name" . }}
    chart: {{ template "tcpmux.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "tcpmux.name" . }}
    release: {{ .Release.Name }}
